import java.awt.*;
import java.awt.event.*;
import javax.swing.*;

public class Properties extends JDialog {

    /** Creates new form Properties */
    private MenuWindow parent;

    public Properties(MenuWindow tempParent) {
        parent = tempParent;
        initComponents();
        pack();
        setSize(200, 350);
    }

    /** This method is called from within the constructor to
	 * initialize the form.
	 * WARNING: Do NOT modify this code. The content of this method is
	 * always regenerated by the FormEditor.
	 */
    public void getDesc() {
        desc_text.setText(parent.canvas[parent.selectedCanvas].getSelected().getDesc());
        name_text.setText(parent.canvas[parent.selectedCanvas].getSelected().getName());
    }

    public void setDesc() {
        parent.canvas[parent.selectedCanvas].getSelected().setDesc(desc_text.getText());
        parent.canvas[parent.selectedCanvas].getSelected().setName(name_text.getText());
    }

    private void initComponents() {
        desc_title = new JTextField();
        desc_text = new JTextArea();
        name_title = new JTextField();
        name_text = new JTextArea();
        desc_button = new JButton();
        getContentPane().setLayout(null);
        setBackground(Color.white);
        setTitle("Properties");
        setName("Properties");
        addWindowListener(new java.awt.event.WindowAdapter() {

            public void windowClosing(java.awt.event.WindowEvent evt) {
                exitForm(evt);
            }
        });
        name_title.setText("Name");
        name_title.setEditable(false);
        desc_title.setText("Description");
        desc_title.setEditable(false);
        getContentPane().add(desc_title);
        getContentPane().add(name_title);
        desc_title.setBounds(0, 50, 190, 20);
        name_title.setBounds(0, 0, 190, 20);
        getContentPane().add(name_text);
        name_text.setBounds(0, 20, 190, 20);
        getContentPane().add(desc_text);
        desc_text.setBounds(0, 80, 190, 180);
        desc_button.setText("Save");
        desc_button.addActionListener(new java.awt.event.ActionListener() {

            public void actionPerformed(java.awt.event.ActionEvent evt) {
                desc_buttonActionPerformed(evt);
            }
        });
        getContentPane().add(desc_button);
        desc_button.setBounds(50, 270, 80, 40);
    }

    private void desc_buttonActionPerformed(java.awt.event.ActionEvent evt) {
        setDesc();
    }

    /** Exit the Application */
    private void exitForm(java.awt.event.WindowEvent evt) {
        hide();
    }

    /**
	* @param args the command line arguments
	*/
    public static void main(String args[]) {
        new Properties(null).show();
    }

    private JTextField desc_title;

    private JTextArea desc_text;

    private JButton desc_button;

    private JTextArea name_text;

    private JTextField name_title;
}
